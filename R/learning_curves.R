# create a function to measure learning rate and avoid overfitting/underfitting
# 1. obtain area under the loss curve (train and test loss)
# 2. test_loss-train_loss=gap.
# 3. run iterations until the gap is very low.
# 4. If the gap starts to increase, then we trained for too long.


